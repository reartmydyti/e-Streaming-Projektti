@model IEnumerable<MovieDB.Models.Producer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Producer_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nacionaliteti_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Producer_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nacionaliteti_ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Producer_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Producer_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Producer_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
